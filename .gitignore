.DS_Store
node_modules/
.env
*.local

# Baked deck — generated by scripts/bake-cards.mjs.
# Deliberately NOT committed: these are NFL/Getty headshots, and this repo is
# public. Caching them on your own machine is one thing; republishing a few
# hundred of them from a public repo is another. Netlify re-bakes at deploy
# time (see netlify.toml), so production still gets the photos.
# Building something private and want them in git? Delete these two lines.
data/cards.js
data/photos/
